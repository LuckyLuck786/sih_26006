import { Area, CartesianGrid, ComposedChart, Line, ReferenceLine, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'

const currency = new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 })

export default function ForecastChart({ series = [], currentRate }) {
  const data = Array.isArray(series) ? series.map((point) => ({ ...point, band: Math.max(0, (point.upper || 0) - (point.lower || 0)), base: point.lower || 0 })) : []
  return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.15em] text-teal-700">Rate outlook</p><h2 className="font-display mt-1 text-xl font-bold text-slate-900">Freight forecast</h2></div><p className="text-xs text-slate-400">14-day planning window</p></div>{data.length === 0 ? <div className="grid h-64 place-items-center text-sm text-slate-400">Forecast data will appear after analysis.</div> : <div className="mt-5 h-72 w-full"><ResponsiveContainer width="100%" height="100%"><ComposedChart data={data} margin={{ top: 10, right: 10, left: 12, bottom: 8 }}><CartesianGrid stroke="#e2e8f0" strokeDasharray="3 3" vertical={false} /><XAxis dataKey="day" tickLine={false} axisLine={false} label={{ value: 'Day', position: 'insideBottom', offset: -4, fill: '#64748b', fontSize: 12 }} /><YAxis tickLine={false} axisLine={false} tickFormatter={(value) => `₹${Math.round(value / 1000)}k`} label={{ value: 'Freight rate (₹)', angle: -90, position: 'insideLeft', fill: '#64748b', fontSize: 12 }} domain={['dataMin - 1000', 'dataMax + 1000']} /><Tooltip formatter={(value, name) => [currency.format(value), name === 'expected' ? 'Expected' : 'Range']} labelFormatter={(day) => `Day ${day}`} /><Area type="monotone" dataKey="base" stackId="range" stroke="none" fill="transparent" /><Area type="monotone" dataKey="band" stackId="range" stroke="none" fill="#99f6e4" fillOpacity={0.55} /><Line type="monotone" dataKey="expected" stroke="#0f766e" strokeWidth={3} dot={false} activeDot={{ r: 5, fill: '#0f766e' }} />{typeof currentRate === 'number' && <ReferenceLine y={currentRate} stroke="#f59e0b" strokeDasharray="6 5" label={{ value: 'Current rate', position: 'insideTopRight', fill: '#b45309', fontSize: 12 }} />}</ComposedChart></ResponsiveContainer></div>}</section>
}